export const env = {
  DATABRICKS_HOST:
    process.env.DATABRICKS_HOST || "",

  DATABRICKS_PAT:
    process.env.DATABRICKS_PAT || "",

  DATABRICKS_SPACE_ID:
    process.env.DATABRICKS_SPACE_ID || "",
};